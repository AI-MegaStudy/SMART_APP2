# 점주 앱 시연 모드 시나리오

작성일: 2026-05-11
최종 갱신: 2026-05-11

## 진입 방식

- 일반 사용자는 보이지 않는다.
- 점주 계정 로그인 후 홈 화면 상단의 점주 인사 영역을 3번 빠르게 탭하면 시작한다.
- 진입 이후 별도 데모 화면으로 이동하지 않고, 실제 앱 페이지 위에만 하이라이트 오버레이를 띄운다.
- 화면 하단에 단계 설명 문구나 진행도 박스를 띄우지 않는다.
- 하이라이트는 실제 누를 수 있는 메뉴, 버튼, 이미지 선택 영역, 입력 컨트롤을 기준으로 표시한다.
- 전체 화면 딤은 사용하지 않는다.
- 화면 이동, 탭 전환, 자동 스크롤 중에는 하이라이트를 즉시 숨긴다.
- 대상 위치가 2프레임 이상 안정된 뒤 좌표를 한 번 측정하고, 그 고정 좌표에만 얇은 반투명 선을 표시한다.
- 하이라이트는 대상 위젯을 실시간 추적하지 않는다. 따라서 이동 중 박스가 따라오거나 좌상단에서 나타나면 결함으로 본다.
- 하이라이트 애니메이션, 전체 딤, 큰 펄스 점, 하단 설명 박스, 진행도 표시는 사용하지 않는다.
- 기본 지연 시간은 화면 전환 약 1.1초, 자동 스크롤 약 0.45초, 대상 강조 약 4초로 둔다.

## 자동 진행 순서

발표 자료의 큰 흐름은 `스마트 계획 -> 데이터 동기화 -> 품질 · 물류 관리 -> 배송/사후 관리`이다.
앱 데모는 이 흐름을 메뉴 소개가 아니라 실제 점주 업무 화면의 조작 상태로 보여준다.

1. 홈 진입
   - 강조 대상: 상단 점주 인사 문구
   - 설명: 점주 이름과 오늘 업무 현황 확인

2. 메뉴 탭
   - 강조 대상: 하단 `메뉴` 탭
   - 설명: 점주 업무 전체 메뉴로 이동

3. 상품 관리 메뉴
   - 강조 대상: `상품 관리` 메뉴

4. 상품 등록 시작
   - 이동: 실제 `상품 관리` 페이지
   - 강조 대상: 우측 상단 `+` 상품 등록 버튼
   - 실제 동작: 실제 상품 목록 페이지로 push

5. 상품 등록 - 품종 선택
   - 이동: 실제 `상품 추가` 페이지
   - 강조 대상: `사과 품종` 드롭다운
   - 데모 상태: `양광` 선택 완료
   - 실제 동작: `ProductAddPage(demoPreset: true)`가 폼 값을 미리 세팅

6. 상품 등록 - 포장 단위 선택
   - 강조 대상: `포장 단위` 드롭다운
   - 데모 상태: `5kg 박스` 선택 완료

7. 상품 등록 - 대표 이미지 선택
   - 강조 대상: 상품 대표 이미지 박스
   - 데모 상태: 갤러리 선택 대신 데모 등록용 양광 이미지가 이미 선택된 상태
   - 실제 동작: OS 사진 피커를 자동 조작하지 않고 프리셋 asset을 로드

8. 상품 등록 - 판매가 조정
   - 강조 대상: `기본 판매가` 업다운 셀렉터
   - 데모 상태: 41,000원 입력 완료

9. 상품 추가 - 등록
   - 강조 대상: 하단 `등록` 버튼
   - 설명: 발표 중 확인 다이얼로그로 화면을 막지 않기 위해 실제 저장 직전 상태까지 보여준다.

10. 수확 예측 메뉴
   - 강조 대상: `수확 예측` 메뉴

11. 수확 예측 - 상품 선택 및 기준값 확인
   - 이동: 실제 `수확 예측` 페이지
   - 강조 대상: `상품` 드롭다운

12. 수확 예측 - 최근 기준 수확량 조정
   - 강조 대상: `최근 기준 수확량` 업다운 셀렉터

13. 수확 예측 - ML 예측 실행
   - 강조 대상: `수확 예측 실행` 버튼
   - 데모 상태: 데모 모드에서 실제 예측 API/fallback 실행 후 예측 결과, 권장 예약량, 권장 가격이 표시된다.
   - 실제 동작: `HarvestSlotPage(demoAutoPredict: true)`가 예측 생성을 자동 실행

14. 수확 슬롯 확정
   - 강조 대상: `수확 슬롯 열기` 버튼
   - 설명: 예측값을 그대로 쓰지 않고 점주가 수확 기간, 예약 가능 수량, 판매가, 고객 안내 문구를 확정한 뒤 연다.
   - 발표 처리: 확인 다이얼로그로 흐름이 멈추지 않도록 버튼 직전 상태까지 보여준다.

15. 주문 현황 메뉴
   - 강조 대상: `주문 현황` 메뉴

16. 주문 현황 - 결제 완료 확인
   - 이동: 실제 `주문 현황` 페이지
   - 강조 대상: `주문` 탭과 `결제 완료` 주문 카드
   - 설명: 고객이 상품 선택 후 결제 완료한 결과가 점주 DB 화면에 들어온 상태를 보여준다.
   - 실제 동작: `OrdersPage(demoMode: true)`가 `주문 / 결제 완료` 필터로 시작

17. 주문 현황 - 예약 상태 확인
   - 강조 대상: `예약` 탭, `예약 유지` 카드, `주문 전환` 카드
   - 데모 상태: 데모 모드에서 주문 탭에서 예약 탭으로 자동 전환된다.
   - 설명: 예약 생성, 예약 유지, 결제 후 주문 전환 상태를 점주 화면에서 확인한다.

18. 발주 승인 메뉴
   - 강조 대상: `발주 승인` 메뉴

19. 발주 승인 목록
   - 이동: 실제 `발주 승인` 페이지
   - 강조 대상: 첫 번째 발주 요청 카드

20. 발주 승인 상세
   - 이동: 실제 `발주 상세` 페이지
   - 강조 대상: `전체 승인`, 첫 품목의 승인 포장/승인 kg 조정 영역, `결정 저장` 버튼
   - 설명: 단순 목록 승인 대신 고객 주문으로 생성된 발주를 품목별 승인 수량까지 확인하는 흐름으로 보여준다.
   - 실제 동작: `ProcurementPage(demoOpenFirst: true)`가 첫 발주 카드를 보여준 뒤 상세로 자동 진입

21. 신선도 검사 메뉴
   - 강조 대상: `신선도 검사` 메뉴

22. 신선도 검사 - 검사 대상 선택
   - 이동: 실제 `신선도 검사` 페이지
   - 강조 대상: `검사 대상 발주 품목` 드롭다운

23. 신선도 검사 - 이미지 표시
   - 강조 대상: 이미지 선택 박스
   - 데모 상태: 데모용 사과 이미지가 이미 선택된 상태
   - 실제 동작: OS 사진 피커 대신 검사 대상 품종에 맞는 demo asset을 로드

24. 신선도 검사 - 분석 실행
   - 강조 대상: 하단 `분석` 버튼
   - 데모 상태: 데모 모드에서 실제 분석 API/fallback 실행 후 결과가 채워진다.
   - 실제 동작: `QualityPage(demoAutoImage: true, demoAutoAnalyze: true)`가 이미지 로드 후 분석을 자동 실행

25. 신선도 검사 - 분석 결과 확인
   - 강조 대상: 추천 등급, 신선도, 색상 점수, 형태 점수 카드 그룹
   - 설명: 멍 확률과 모델 판정도 같은 화면에서 확인한다.

26. 신선도 검사 - 점주 최종 판정
   - 강조 대상: `점주 확정 등급`, `점주 판정` 드롭다운
   - 설명: 모델 결과를 보조 자료로 보고 점주가 최종 출고 판단을 확정한다.

27. 신선도 검사 - 저장
   - 강조 대상: 하단 `저장` 버튼
   - 설명: 발표 중 확인/스낵바 간섭을 줄이기 위해 저장 버튼까지 보여준다.

28. 배송 관리 메뉴
   - 강조 대상: `배송 관리` 메뉴

29. 배송 관리 - 포장 코드 스캔
   - 이동: 실제 `배송 관리` 페이지
   - 강조 대상: 우측 상단 스캔 버튼
   - 데모 상태: 승인 발주 상품이 선택되고 송장번호/박스/중량이 자동 입력된 상태
   - 실제 동작: `ShipmentPage(demoAutofill: true)`가 첫 배송 가능 발주를 선택하고 포장 코드 스캔 결과처럼 송장번호를 채움

30. 배송 관리 - 발주 상품 선택
   - 강조 대상: `발주 승인 상품` 드롭다운

31. 배송 관리 - 송장번호 확인
   - 강조 대상: `송장번호` 입력 필드

32. 배송 관리 - 등록
   - 강조 대상: 하단 `등록` 버튼

33. 반품 · 환불 메뉴
   - 강조 대상: `반품 · 환불 관리` 메뉴

34. 반품 목록
   - 이동: 실제 `반품 · 환불 관리` 페이지
   - 강조 대상: 첫 번째 반품 요청 카드
   - 실제 동작: `ReturnPage(demoOpenFirst: true)`가 첫 반품 요청을 보여준 뒤 상세로 자동 진입

35. 반품 상세 - 고객 첨부 이미지
   - 이동: 실제 `반품 상세` 페이지
   - 강조 대상: 고객 첨부 이미지

36. 반품 상세 - 승인 금액
   - 강조 대상: `승인 금액` 입력 필드

37. 반품 상세 - 승인
   - 강조 대상: 하단 `승인` 버튼
   - 설명: 배송 후 사후 관리까지 이어지는 환불/반품 처리 지점을 보여준다.

## 구현 기준

- 데모 컨트롤러: `lib/demo/owner_demo_manager.dart`
- 실제 페이지 대상 키: `DemoTargetKeys`
- 데모 시작 트리거: `lib/view/home.dart`의 홈 상단 점주 인사 영역 3회 빠른 탭
- 메뉴 키 연결: `lib/view/menu_page.dart`
- 주문/예약 흐름: `lib/view/orders_page.dart`
- 발주 상세 흐름: `lib/view/procurement_page.dart`, `lib/view/procurement_detail_page.dart`
- 신선도 검사 흐름: `lib/view/quality_page.dart`
- 배송 등록 흐름: `lib/view/shipment_page.dart`
- 반품/환불 상세 흐름: `lib/view/return_page.dart`

## 하이라이트 표시 규칙

- `_focus()`는 항상 먼저 현재 하이라이트를 숨긴다.
- 대상 `GlobalKey.currentContext`가 생길 때까지 짧게 대기한다.
- `Scrollable.ensureVisible()`로 실제 대상이 보이는 위치까지 이동한다.
- 이동 후 `RenderBox.localToGlobal()` 좌표가 안정될 때까지 기다린다.
- 안정된 `Rect`를 오버레이에 넘겨 고정 좌표로만 표시한다.
- 다음 대상, 화면 이동, 루트 복귀, 새 페이지 push 시 하이라이트는 다시 숨긴다.
- 대상당 표시 시간은 4초다.

## 발표 검수 체크리스트

- 데모가 별도 fake page로 이동하지 않고 실제 점주 앱 화면 위에서 진행되는가.
- 메뉴만 훑지 않고 상품 등록, 예측 실행, 주문/예약 확인, 발주 상세, 신선도 분석, 배송 입력, 반품 상세가 이어지는가.
- 이동/스크롤 중 하이라이트가 완전히 사라지는가.
- 하이라이트가 좌상단에 잠깐 뜨거나 이동 대상을 따라오지 않는가.
- 하단 설명 박스, 진행도 표시, 전체 딤 레이어, 큰 펄스 점이 없는가.
- OS 사진 피커 자동 조작 없이 프리셋 이미지가 안정적으로 표시되는가.
- 고객 앱은 열지 않되, 점주 앱 `주문 현황`에서 고객 예약/결제 결과를 확인할 수 있는가.

## 최근 검증

- `dart format` 실행: 데모 관련 Dart 파일
- `flutter analyze`: 통과
- `flutter test`: 통과
- `flutter build ios --simulator --dart-define=API_BASE_URL=http://127.0.0.1:8000/api/v1`: 통과
- iOS 시뮬레이터 설치/실행: 완료

## 이미지 세트

### 현재 앱 표시용

- `assets/images/owner_demo/demo_yanggwang_product.png`
- `assets/images/owner_demo/demo_fuji_product.png`
- `assets/images/owner_demo/demo_apple_shipping_box.png`
- `assets/images/owner_demo/demo_damaged_return_box.png`

### 데모 등록/갤러리 선택용

- `assets/images/owner_demo/demo_register_yanggwang_product.png`
- `assets/images/owner_demo/demo_register_fuji_product.png`
- `assets/images/owner_demo/demo_register_apple_shipping_box.png`
- `assets/images/owner_demo/demo_register_damaged_return_box.png`

두 세트 모두 iOS 시뮬레이터 갤러리에 등록했다.
